require 'spec_helper'

describe FxInfoReader do
  it 'has a version number' do
    expect(FxInfoReader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
